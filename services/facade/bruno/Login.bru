meta {
  name: Login
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

auth:bearer {
  token: 
}

body:json {
  {
    "username": "admin",
    "password": "admin"
  }
}
