generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/@prisma/scheduler-client"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model EInkJob {
  id          Int     @id @default(autoincrement())
  name        String?
  cron        String
  target      String
  command     String
  commandType String
  commandArgs String  @default("[]")
  hasSeconds  Boolean @default(false)
  priority    Int     @default(0)
  cycle       Int     @default(0)
  shouldCycle Boolean @default(false)
  disabled    Boolean @default(false)

  @@unique([target, cron])
}
